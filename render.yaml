services:
  - type: web
    name: chatbot-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn Main:app
    envVars:
      - key: FLASK_ENV
        value: production
